<%- include("header") -%>

<table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col"> Customer ID Sender</th>
        <th scope="col">Sender Name</th>
        <th scope="col">Customer ID Reciever </th>
        <th scope="col">Reciever Name</th>
        <th scope="col">Amount Transferred</th>
        <th scope="col">Status</th>
        <th scope="col">Date & Time</th>
      </tr>
    </thead>
    <tbody>
        <% fullList.forEach(function(user) { %>
          <tr>
              <th scope="row" ><%= user.customer_id1 %></th>
              <td><%= user.name1 %></td>
              <td class="disp"><%= user.customer_id1 %></td>
              <td> <%= user.name2%></td>
              <td>₹ <%= user.debit%></td>
              <td> <%= user.Status%></td>
              <td> <%= user.Time%></td>
              

             
          </tr>
          <%  }) %>
      </tbody>
    </table>